server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/project_db
    username: postgres
    password: Shiva@123

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

  mail:
    host: smtp.gmail.com
    port: 587
    
    password:           # ← 16-char App Password here
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

jwt:
  
  expiration: 3600000
  refresh-expiration: 604800000

ai:
  

notification:
  from-email: shivakrishna0504@gmail.com
  from-name: CollabSphere
  app-url: http://localhost:5173

cors:
  allowed-origins: http://localhost:5173,http://localhost:3000

resilience4j:
  circuitbreaker:
    instances:
      auth-service:
        registerHealthIndicator: true
        slidingWindowSize: 5
        minimumNumberOfCalls: 3
        failureRateThreshold: 50
        waitDurationInOpenState: 10s
        permittedNumberOfCallsInHalfOpenState: 2


file:
  upload-dir: uploads/projects